import React from 'react';
import ReactDOM from 'react-dom/client';
import DCCarousel from './App';
import './index.css'; 

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DCCarousel />
  </React.StrictMode>,
);
